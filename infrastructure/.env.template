# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Pulumi Configuration
PULUMI_CONFIG_PASSPHRASE=your-passphrase
PULUMI_BACKEND_URL=s3://footprint-pulumi-state-{account-id}?region=us-east-1

# Application Secrets (for production, store in AWS Secrets Manager)
JWT_SECRET=generate-a-secure-random-string
APPLE_TEAM_ID=your-apple-team-id
APPLE_KEY_ID=your-apple-key-id
APPLE_PRIVATE_KEY=your-apple-private-key-contents

# GitHub Secrets Required:
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY
# - AWS_REGION
# - PULUMI_CONFIG_PASSPHRASE

# Deployment Environments:
# - dev: Local deployment only (run `pulumi up` manually)
# - staging: Auto-deployed on merge to main branch
# - prod: Deployed on git tag (e.g., v1.0.0)
